var form = $("<%= "#edit_atividade_#{@atividade.id}" %>");
<% if @error_message.blank? %>
  formSaved(form);
  updateTrelloData("<%= @atividade.trello_id %>", true, false, 0);
<% else %>
  hideSpinner(form);
  alert('<%= @error_message %> Clique em "Editar cartão" para solucionar o problema e tente novamente.');
<% end %>